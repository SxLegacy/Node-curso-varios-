const fs = require('fs');
/* Una vez importado podremos comenzar a usar los métodos que este módulo nos ofrece: */
Fs.readFile(path, callback); //usamos el método .readFile para leer archivos 
Fs.writeFile(path, content, callback) //usamos el método .writeFile para escribir archivos
Fs.unlink(path, callback) //usamos el método .unlink para eliminar archivos

